import type { AgreementArtifactType, AgreementDocumentSource, AgreementRenderer } from "@/application/agreements/agreement-renderer"; import { HtmlAgreementRenderer } from "./html-agreement-renderer"; import { PdfAgreementRenderer } from "./pdf-agreement-renderer";
export class DefaultAgreementRenderer implements AgreementRenderer { constructor(private readonly html = new HtmlAgreementRenderer(), private readonly pdf = new PdfAgreementRenderer()) {} render(source: AgreementDocumentSource, type: AgreementArtifactType) { return type === "HTML" ? this.html.render(source) : this.pdf.render(source); } }
