import type { AgreementDocumentSource } from "@/application/agreements/agreement-renderer"; import { complete } from "./agreement-renderer-support";
const esc = (value: string) => value.replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&#39;");
export class HtmlAgreementRenderer { render(source: AgreementDocumentSource) { const scope = source.scope.map(({ heading, body }) => `<section><h2>${esc(heading)}</h2><p>${esc(body)}</p></section>`).join(""); const placeholders = ["Effective Date", "Confidentiality", "Intellectual Property", "Limitation of Liability", "Governing Law", "Termination"].map((title) => `<section><h2>${title}</h2><p>To be completed through the governed Agreement workflow.</p></section>`).join(""); const html = `<!doctype html><html lang="en"><head><meta charset="utf-8"><title>${esc(source.agreementNumber)}</title></head><body><main><h1>Service Agreement</h1><p>Agreement ${esc(source.agreementNumber)} · Version 1</p><p>Generated ${esc(source.generatedAt)}</p><h2>Parties</h2><p>${esc(source.companyName)} and ${esc(source.clientName)}</p><p>Proposal ${esc(source.proposalNumber)} · Version ${source.proposalVersionNumber}</p><h2>Scope of Services</h2>${scope}<h2>Pricing Summary</h2><p>${esc(source.pricingSummary)}</p><h2>Payment Terms</h2><p>${esc(source.paymentTerms)}</p><h2>Term</h2><p>${esc(source.term)}</p>${placeholders}<section><h2>Signatures</h2><p>Client: ____________________ Date: __________</p><p>Cotarion Representative: ____________________ Date: __________</p></section></main></body></html>`; return complete(source, "HTML", "text/html; charset=utf-8", Buffer.from(html)); } }
