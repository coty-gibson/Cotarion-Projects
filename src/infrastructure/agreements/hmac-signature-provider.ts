import { createHash, createHmac } from "node:crypto"; import type { SignatureProvider } from "@/application/agreements/signature-provider";
export class HmacSignatureProvider implements SignatureProvider { readonly method="INTERNAL_SECURE_LINK" as const; constructor(private readonly secret:string){if(Buffer.byteLength(secret)<32)throw new Error("Signature secret must contain at least 32 bytes.");} issue(companyId:string,requestIdentity:string){const token=createHmac("sha256",this.secret).update(`agreement-signature-v1:${companyId}:${requestIdentity}`).digest("base64url");return{token,digest:this.digest(token)};} digest(token:string){return createHash("sha256").update(token).digest("hex");} }
