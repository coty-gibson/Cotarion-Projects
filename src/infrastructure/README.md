# Infrastructure Layer

This layer will contain PostgreSQL access, Prisma, authentication provider integration, object storage, PDF rendering, and future external integrations.

Sprint 1 intentionally implements no infrastructure integrations.
