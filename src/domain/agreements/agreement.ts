export type AgreementStatus = "GENERATED" | "READY_FOR_SIGNATURE";
export interface AgreementState { readonly id: string; readonly companyId: string; readonly proposalId: string; readonly proposalVersionId: string; readonly proposalRepresentationId: string; readonly clientDecisionId: string; readonly agreementNumber: string; readonly agreementVersion: 1; readonly status: AgreementStatus; readonly generatedAt: string; readonly generatedByUserId: string; }
export class Agreement { private constructor(private readonly value: AgreementState) {} static generated(value: AgreementState) { if (value.agreementVersion !== 1) throw new Error("Initial Agreement Version must be 1."); return new Agreement(structuredClone(value)); } get state() { return structuredClone(this.value); } }
